# Ansible Inventory for PostgreSQL Compliance Scanning

[postgresql_scanners]
# Add your scanner hosts here
localhost ansible_connection=local

# Example remote scanner hosts:
# scanner01.example.com ansible_user=ansible_user
# scanner02.example.com ansible_user=ansible_user

[postgresql_scanners:vars]
# Default PostgreSQL connection parameters
# Override these in host_vars files or group_vars files
pg_host=localhost
pg_port=5432
pg_user=test_user
pg_password=P@ssw0rd
pg_database=test_db
pg_superuser=postgres
pg_superuser_password=P@ssw0rd123

# Scanner configuration
ansible_python_interpreter=/usr/bin/python3

[all:vars]
# Global settings
gather_facts=true